# === Discord Bot ===
# Token du bot Discord (ne jamais le divulguer)
TOKEN=TON_TOKEN_DISCORD
# ID de l'application (Application client) utilisée pour les slash commands
CLIENT_ID=TON_CLIENT_ID
# Secret de l'application (pour OAuth)
CLIENT_SECRET=TON_CLIENT_SECRET
# (Optionnel) ID de la guilde de développement pour enregistrement local rapide
GUILD_ID=TON_GUILD_ID
# (Optionnel) salon de logs global
LOG_CHANNEL_ID=TON_LOG_CHANNEL_ID

# === OAuth2 Dashboard / Session ===
# URL de callback OAuth2 (ex: https://example.com/auth/callback)
REDIRECT_URI=TON_REDIRECT_URI
# Secret pour express-session (en production : utiliser un secret fort)
SESSION_SECRET=une_chaine_secrete_pour_session

# === Google / SteamTools (pour la commande steamgame) ===
# Email du compte de service Google (service account)
GOOGLE_CLIENT_EMAIL=service-account@project.iam.gserviceaccount.com
# Clé privée (PEM). Stocker les sauts de ligne comme `\\n` dans le .env,
# le code appelle `.replace(/\\n/g, '\n')` pour restaurer la clé.
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\\nMIIB...\\n-----END PRIVATE KEY-----\\n"
# ID du dossier Google Drive contenant les ZIP / .lua
STEAMTOOLS_FOLDER_ID=TON_STEAMTOOLS_FOLDER_ID

# === Runtime / Environnements ===
# Mode d'exécution (ex: development | production | debug)
APP_ENVIRONMENTS=development

# === Sécurité / notes ===
# - Ne versionnez jamais ce fichier (copiez-le en `.env` et remplissez les valeurs).
# - Pour la clé Google, conservez les `\\n` dans le fichier .env, ne les remplacez pas par de vrais retours ligne.
